<% content_for :title, "New brand" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New brand</h1>

  <%= render "form", watch_brand: @watch_brand, country_options: @country_options %>

  <%= link_to "Back to brands", watch_brands_path, class: "btn" %>
</div>
