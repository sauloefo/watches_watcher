<div id="<%= dom_id brand %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
    <strong class="block font-medium mb-1">Name:</strong>
    <%= brand.name %>
  </div>

  <div>
    <strong class="block font-medium mb-1">Verified at:</strong>
    <%= brand.verified_at&.strftime("%Y-%m-%d %H:%M:%S UTC") || "Not verified" %>
  </div>
</div>
